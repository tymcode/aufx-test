/**
 * Host chrome widgets used by MainContent (LEDs, transport, pickers, sliders).
 * Implementations are inline in the header — these types are only referenced from
 * MainContent and are awkward to split without pulling JUCE UI headers everywhere.
 */
#include "HostChromeControls.h"
